# Windows noise
desktop.ini
Thumbs.db

# Editor/OS scratch
*.tmp
~$*

# Test tooling (dev-only; the app itself stays build-free)
node_modules/
test-results/
playwright-report/
.playwright/

# Firebase emulator artifacts (local, dev-only)
*-debug.log
firebase-debug.*.log
.firebase/
